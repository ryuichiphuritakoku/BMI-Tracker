
  # BMI Tracker Prototype

  This is a code bundle for BMI Tracker Prototype. The original project is available at https://www.figma.com/design/T1ayFkoN4x1Mw9BeSrXfuv/BMI-Tracker-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  "# BMI-Tracker" 
"# BMI-Tracker" 
